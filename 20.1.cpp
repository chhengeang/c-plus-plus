#include<iostream>
#include<conio.h>
int main()
{
	int a,b,max;
	char ch;
	again:
		system("cls");
		cout<<"enter a="; cin>>a;
		cout<<"enter b="; cin>>b;
		if(a,b)
		max=a;
		else
		max=b;
		cout<<"maximum="<<max<<endl;
		cout<<"\npress enter to cntinue, any key to exit";
		ch=getch();
		if(ch==13)
		gpt0 again;
}
